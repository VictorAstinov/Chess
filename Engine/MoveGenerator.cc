#include "MoveGenerator.h"

std::vector<Move>& MoveList::generate_moves(Position& pos) {

  // make sure movelist is empty
  movelist.clear();
  EngineColor side = pos.toMove();
  BitBoard pieces = ;

  return movelist;
}